class Solution {
//1152. Analyze User Website Visit Pattern
//wrong idea: 3-seq web visited by differnet user
//right idea: seq ascending by time
    public List<String> mostVisitedPattern(String[] username, int[] timestamp, String[] website) {
		//[user,time,web]
        List<List<String>> sessions;
		sessions.sort();//by time
		//{user-{web1,web2}}
		Map<String, List<String>> visited
		
		Map<String, Integer> sequence = new HashMap<>();//(sequence, count)
		for(name){
			if(list.size()<3)continue;
			Set<String> subseqences
			for(seq){
				if(>maxCount){
					seq;maxCount;maxSeq;
				}else if (sequence.get(seq) == maxCount && seq.compareTo(maxseq) < 0) maxseq = seq;
			}
		}		
    }
	public Set<String> subseqence(List<String> list){
		//find all subseq
		for(){for(){for(){
			res.add(list.get(i)+","+list.get(j)+","+list.get(k));
		}}}
	}
}